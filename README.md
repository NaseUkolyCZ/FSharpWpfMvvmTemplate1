FSharpWpfMvvmTemplate1
======================

Multi-project template that generates a WPF solution with logical separation between View (C#), ViewModel (F#), Model (F#), and Repository (F#). Using Modern UI.
